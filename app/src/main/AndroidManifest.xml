<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testivacc">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TESTIVACC">
        <activity android:name=".Activities.SERVICES"></activity>
        <activity android:name=".Activities.Profile"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".Activities.About" />
        <activity android:name=".Activities.Language"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".Activities.Doctors"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".Activities.Fourth"
            android:theme="@style/AppTheme.NoActionBar"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".Activities.Signin"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".Activities.Login"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>